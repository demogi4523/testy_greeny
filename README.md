# Задачи на вакансию стажера(повышенной сложности)

## Запуск
```bash
> docker-compose build && docker-compose up
> <ctrl-D>
```
После запуска становится доступна главная страница проекта по адресу http://localhost:8000


