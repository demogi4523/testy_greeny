version: '3.8'

services:
  web:
    build: app
    container_name: fastapi
    command: bash -c "uvicorn main:app --host 0.0.0.0"
    ports:
      - "8000:8000"
    environment:
      - MODE=prod
    depends_on:
      - db
    networks:
      - space_x_net

  db:
    image: "postgres"
    container_name: ps
    ports:
      - "5432:5432"

    restart: always
    networks:
      - space_x_net

networks:
  space_x_net:
    driver: bridge
